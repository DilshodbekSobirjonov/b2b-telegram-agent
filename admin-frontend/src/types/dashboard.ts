export interface DashboardStats {
  totalRevenue: number;
  activeBots: number;
  appointments: number;
  aiEfficiency: number;
}

export interface ActivityChartData {
  date: string;
  bookings: number;
}
